# assets

## Purpose

Brand assets for the Talaria project — a coloured winged-sandal
lock-up and its derived sizes. The lock-up pairs a vector-path
glyph (a 3-feather winged sandal: 3 smooth cubic-Bézier feathers
per side on a wide flat rectangular base) with a serif wordmark.
The bicolour design uses gold on the upper portion and amber on
the lower portion of both the glyph and the wordmark, with the
glyph band and the wordmark band on the same horizontal line and
the glyph base bar bottom sitting on the wordmark baseline. The
lockup renders on a transparent background so it can be placed on
any surface.

## Ownership

- The glyph is built from vector `<path>` primitives in `build_logo.py`
  (no ASCII art, no fonts in the glyph).  Path data is generated
  programmatically from the geometry constants at the top of the file.
- The wordmark uses an SVG `<text>` element (Georgia / Times New Roman
  serif fallback) with a clipPath-based bicolour fill (gold top, amber
  bottom band) — no font is bundled.
- Source SVGs are the source of truth: `logo.svg` (lock-up,
  transparent bg) and `logo-mark.svg` (square mark only, transparent
  bg).  Both render with a transparent background so the lockup can
  be placed on any surface.
- `build_logo.py` regenerates the SVGs from the geometry constants.
- `drafts/` contains exploratory redesigns (14 styles) generated by
  `build_logo.py` with no flame trail.  Drafts are for review only —
  none of them is the production lockup until promoted.

## Local Contracts

- **Palette** — the design uses the Hermes Agent palette: gold
  `#ffc72c` primary fill; amber `#f9a23a` bottom band.  All
  renders use a transparent background (no navy, no other
  background colour) so the lockup can be placed on any surface.
- **Bicolour** — the production glyph AND the wordmark both
  carry a bicolour band (gold on top, amber on the bottom).
  The wordmark uses gold-on-top with a clipPath-painted amber
  bottom band covering 36% of the cap-height.  The glyph uses
  the same bicolour via a clipPath: a second amber copy of each
  wing path is painted on top of the gold wings and clipped to
  the lockup band region, producing an amber strip across the
  lower portion of the wings and the base bar.  The bands sit
  on the same y line so the amber strip is one continuous
  horizontal line across both halves of the lockup.
- **Geometry** — production glyph is the `solid_3` winged-sandal:
  two symmetric half-silhouettes (left + right) that meet at the
  centreline x=120.  Each half is a SINGLE closed `<path>` whose
  curves form 3 smooth cubic-Bézier feathers flowing down to the
  BASE BAR TOP (y_local=170), then continuing along the base bar
  bottom (y_local=184) and back up the centreline.  The wings +
  base bar are ONE integral piece — the base bar is part of the
  wing path, not a separate `<rect>`.  There is NO pedestal
  between the wings and the base bar — the lower feathers taper
  smoothly into the bar with no visible flat edge or seam.  Path
  data is in `_solid_3_paths()` (also returns the base bar
  geometry) in `build_logo.py`.  The standalone mark uses the
  same path data and the same combined-silhouette structure.
- **Alignment** — the production glyph is translated in the lockup
  so the base bar bottom (local y=184) sits exactly on the
  wordmark baseline.  The bicolour band on the glyph and on the
  wordmark are aligned (band_top_lockup and band_height match the
  wordmark band math; glyph-local coords are derived by
  subtracting origin_y).  Position is via
  `_glyph_origin_y_for_baseline()`.
- **Wordmark** — Georgia / Times New Roman serif, `WORDMARK_FONT =
  168`, letter-spacing 12, weight 700, fully visible (no right-edge
  clipping) in every render.  The wordmark is identical across the
  production lockup and every draft.
- `build_logo.py` MUST be the only place where the glyph geometry is
  defined.  Do not hand-edit the rendered SVG files.

## Work Guidance

- Regenerate SVGs (production + drafts):
  `python3 build_logo.py`.
- Regenerate PNGs:
  `inkscape logo.svg --export-type=png --export-filename=logo-NNN.png -w NNN -h NNN`
  (lockup aspect ratio is roughly 1548:284).  Or with ImageMagick:
  `convert -density 200 -background none logo.svg -resize 1024x logo-1024.png`.
- To redesign the glyph, edit the geometry constants inside
  `render_glyph()` and each `elif style == "<name>":` branch of
  `_draught_glyph()` in `build_logo.py`, then re-run the script.
  The shared base-bar constants live at the top of `_draught_glyph`.
- To change the wordmark style, edit `render_wordmark()` and
  `WORDMARK_*` constants.
- To add a new draft style, add a branch to `_draught_glyph()` in
  `build_logo.py` and append the style name to `DRAFT_STYLES`.  The
  draft will be regenerated automatically.

## Verification

- Visually inspect `logo-1024.png` before commit: TALARIA must be
  fully visible (no right-edge clipping), glyph + wordmark vertically
  centred, three feathers per side clearly readable, the GLYPH MUST
  carry the BICOLOUR RIBBON (gold upper wings, amber band cutting
  across the lower portion of the wings and the base bar), the
  base bar MUST be part of the same closed wing path (one integral
  piece — no separate pedestal between wings and bar), the lower
  feathers MUST taper smoothly into the base bar with no visible
  flat edge or seam, the amber strip on the glyph MUST sit on the
  SAME y line as the amber band on TALARIA, the base bar bottom
  must sit on the wordmark baseline.
- Visually inspect `logo-mark-256.png`: glyph must read as a
  winged-sandal silhouette on its own (without the wordmark), with
  gold upper wings, an amber bicolour ribbon through the lower
  portion, and a single amber base bar at the bottom (no separate
  pedestal).
- After any geometry change, regenerate and inspect both the lockup
  PNG and the standalone mark PNG.
- Visually inspect every draft in `drafts/logo-draft-*.png` (render
  with `convert -density 200 -background none draft.svg -resize 1024x
  draft.png`) before promoting a draft to the production lockup.

## Child DOX Index

- `logo.svg`, `logo-256.png`, `logo-512.png`, `logo-1024.png` — primary lock-up (transparent background). Glyph is the `solid_3` winged-sandal: a single closed `<path>` per side that flows 3 feathers down into the base bar as one integral piece (no separate pedestal). Rendered in gold with an amber bicolour ribbon overlay (clipPath) cutting across the lower portion of the wings and the base bar. The ribbon aligns with the band on TALARIA. The base bar bottom sits on the wordmark baseline.
- `logo-mark.svg`, `logo-mark-128.png`, `logo-mark-256.png` — square mark only (transparent background). Uses the same `solid_3` winged-sandal glyph (gold + amber ribbon, single-piece combined silhouette).
- `drafts/logo-draft-minimal.svg` + `.png` — slim single-feather wings.
- `drafts/logo-draft-bold.svg` + `.png` — chunky angular feathers.
- `drafts/logo-draft-ornate.svg` + `.png` — heraldic / filigree wings.
- `drafts/logo-draft-monoline.svg` + `.png` — outline-only wings, 2 feathers per side (baseline).
- `drafts/logo-draft-monoline_3.svg` + `.png` — outline-only wings, 3 feathers per side.
- `drafts/logo-draft-monoline_4.svg` + `.png` — outline-only wings, 4 feathers per side.
- `drafts/logo-draft-monoline_5.svg` + `.png` — outline-only wings, 5 feathers per side.
- `drafts/logo-draft-monoline_6.svg` + `.png` — outline-only wings, 6 feathers per side (densest fan).
- `drafts/logo-draft-solid_3.svg` + `.png` — solid-filled variant of monoline_3 (3 feathers per side, filled not stroked). Uses the standard glyph-bbox band (NOT aligned with the wordmark).
- `drafts/logo-draft-solid_3_aligned.svg` + `.png` — solid_3 at full size, with the bicolour band and glyph bottom aligned to the wordmark band and baseline.
- `drafts/logo-draft-solid_3_baseless.svg` + `.png` — early baseless attempt (basement dropped, lower feather redesigned as a narrow tail).
- `drafts/logo-draft-solid_3_moved.svg` + `.png` — solid_3 at full size, translated upward so the base bar bottom sits on the wordmark baseline (band uses the standard glyph-bbox rule, so the band and wordmark band are at different y positions).
- `drafts/logo-draft-palm_frond.svg` + `.png` — palm-frond silhouette: multiple curved frond-like shapes radiating from a central area, sitting on a wide flat rectangular base. Band-aligned with the wordmark. (Was the production glyph before the current winged-sandal production.)
- `drafts/logo-draft-emblem.svg` + `.png` — wings inside a roundel frame.
- `build_logo.py` — SVG source generator (single source of truth).
